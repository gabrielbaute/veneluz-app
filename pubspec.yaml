name: veneluz_app
description: "A android client for Veneluz server"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.2

dependencies:
  dio: ^5.11.1
  flutter:
    sdk: flutter
  go_router: ^18.0.1
  package_info_plus: ^10.2.1
  provider: ^6.1.5+1
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_post_build: ^1.3.0

flutter:
  uses-material-design: true
